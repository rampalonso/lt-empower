const Badge = ({ text }) => (
  <span className="inline-block bg-primary text-white text-xs px-2 ml-3 rounded-full uppercase font-semibold tracking-wide md:ml-1">
    {text}
  </span>
)

export default Badge